apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sonarrs.devopsarr.io
spec:
  group: devopsarr.io
  names:
    categories: []
    kind: Sonarr
    plural: sonarrs
    shortNames:
    - son
    singular: sonarr
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SonarrSpec via `CustomResource`
        properties:
          spec:
            description: Define the Custom Resource Definition (CRD) for our operator
            properties:
              message:
                type: string
              replicaCount:
                format: int32
                nullable: true
                type: integer
            required:
            - message
            type: object
          status:
            description: Status object for our CRD
            nullable: true
            properties:
              conditions:
                items:
                  description: Condition type for tracking the status of the resource
                  properties:
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observed_generation:
                format: int64
                nullable: true
                type: integer
              ready:
                nullable: true
                type: boolean
            required:
            - conditions
            type: object
        required:
        - spec
        title: Sonarr
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sonarrqualityprofiles.devopsarr.io
spec:
  group: devopsarr.io
  names:
    categories: []
    kind: SonarrQualityProfile
    plural: sonarrqualityprofiles
    shortNames:
    - sqp
    singular: sonarrqualityprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.cutoff
      name: Cutoff
      type: integer
    - jsonPath: .status.id
      name: ID
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SonarrQualityProfileSpec via `CustomResource`
        properties:
          spec:
            description: |-
              SonarrQualityProfile represents a quality profile in Sonarr
              Quality profiles define which qualities are acceptable and their priority
            properties:
              cutoff:
                default: 0
                description: Quality ID to use as cutoff
                format: int32
                type: integer
              cutoffFormatScore:
                description: Cutoff format score
                format: int32
                nullable: true
                type: integer
              formatItems:
                default: []
                description: Format items (custom formats with scores)
                items:
                  properties:
                    format:
                      description: Custom format ID
                      format: int32
                      nullable: true
                      type: integer
                    name:
                      description: Format name
                      nullable: true
                      type: string
                    score:
                      default: 0
                      description: Score for this format
                      format: int32
                      type: integer
                  type: object
                type: array
              minFormatScore:
                description: Minimum format score
                format: int32
                nullable: true
                type: integer
              minUpgradeFormatScore:
                description: Minimum upgrade format score
                format: int32
                nullable: true
                type: integer
              name:
                description: Quality profile name
                type: string
              qualityGroups:
                description: Ordered list of quality groups
                items:
                  properties:
                    id:
                      description: Quality group ID
                      format: int32
                      nullable: true
                      type: integer
                    name:
                      description: Quality group name
                      nullable: true
                      type: string
                    qualities:
                      description: Ordered list of qualities in this group
                      items:
                        properties:
                          id:
                            description: Quality ID
                            format: int32
                            nullable: true
                            type: integer
                          name:
                            description: Quality name
                            nullable: true
                            type: string
                          resolution:
                            description: Resolution
                            format: int32
                            nullable: true
                            type: integer
                          source:
                            description: Source type
                            nullable: true
                            type: string
                        type: object
                      type: array
                  required:
                  - qualities
                  type: object
                type: array
              sonarrInstanceRef:
                description: Reference to the SonarrInstance
                properties:
                  name:
                    default: ''
                    description: Name of the SonarrInstance resource
                    type: string
                  namespace:
                    description: Namespace of the SonarrInstance (optional, defaults to same namespace)
                    nullable: true
                    type: string
                type: object
              upgradeAllowed:
                default: false
                description: Whether upgrades are allowed
                type: boolean
            required:
            - name
            - qualityGroups
            - sonarrInstanceRef
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                description: Current conditions
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              id:
                description: Sonarr Quality Profile ID
                format: int32
                nullable: true
                type: integer
              observedGeneration:
                default: 0
                description: Observed generation
                format: int64
                type: integer
            type: object
        required:
        - spec
        title: SonarrQualityProfile
        type: object
    served: true
    storage: true
    subresources:
      status: {}
